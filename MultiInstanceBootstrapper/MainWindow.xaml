<Window x:Class="MultiInstanceBootstrapper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MultiInstanceBootstrapper"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MultiInstance Bootstrapper v1.2.1"
        Width="1100" Height="780"
        MinWidth="900" MinHeight="680"
        Background="#0D0D0D"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#16213E" CornerRadius="0,0,12,12" Padding="24,16">
            <DockPanel>
                <Border DockPanel.Dock="Left" Width="44" Height="44" CornerRadius="10" Background="#E94560">
                    <TextBlock Text="RB" Foreground="#FFFFFF" FontFamily="Segoe UI" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel DockPanel.Dock="Left" Margin="12,0,0,0">
                    <TextBlock Text="MultiInstance Bootstrapper" Foreground="#FFFFFF" FontFamily="Segoe UI" FontWeight="Bold" FontSize="20" />
                    <TextBlock Text="{Binding VersionText}" Foreground="#A0A0B0" FontFamily="Segoe UI" FontSize="12" />
                </StackPanel>
                <DockPanel DockPanel.Dock="Right" LastChildFill="False">
                    <TextBlock Text="{Binding UpdateStatusText}" VerticalAlignment="Center" Foreground="#00C853" FontFamily="Segoe UI" FontSize="12" Margin="0,0,10,0" />
                    <Button Content="Check Updates" Width="120" Height="36" Command="{Binding CheckUpdateCommand}" />
                </DockPanel>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,16">
            <Grid Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- User Info -->
                <Border Grid.Row="0" Background="#1A1A2E" BorderBrush="#16213E" BorderThickness="1" CornerRadius="16" Padding="24" Margin="20,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="80" Height="80" CornerRadius="40" Background="#0F3460">
                            <Border.Clip>
                                <EllipseGeometry Center="40,40" RadiusX="40" RadiusY="40" />
                            </Border.Clip>
                            <TextBlock Text="{Binding AvatarInitial}" Foreground="#FFFFFF" FontFamily="Segoe UI" FontWeight="Bold" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" Margin="20,0,0,0">
                            <TextBlock Text="{Binding DisplayName}" Foreground="#FFFFFF" FontFamily="Segoe UI" FontWeight="Bold" FontSize="20" />
                            <TextBlock Text="Roblox User" Foreground="#A0A0B0" FontFamily="Segoe UI" FontSize="13" Margin="0,4,0,0" />
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Active Instances: " Foreground="#A0A0B0" FontFamily="Segoe UI" FontSize="12" />
                                <TextBlock Text="{Binding ActiveCountText}" Foreground="#E94560" FontFamily="Segoe UI" FontSize="12" FontWeight="Bold" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Section Title -->
                <TextBlock Grid.Row="1" Text="Your Instances" Foreground="#FFFFFF" FontFamily="Segoe UI" FontWeight="Bold" FontSize="18" Margin="20,24,0,8" />

                <!-- Instance Cards -->
                <ItemsControl Grid.Row="2" ItemsSource="{Binding Instances}" Margin="20,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#1A1A2E" BorderBrush="#16213E" BorderThickness="1" CornerRadius="16" Padding="20" Width="320" Margin="0,0,16,16">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Width="40" Height="40" Fill="#0F3460" />
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="#FFFFFF" FontFamily="Segoe UI" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="12,0,0,0" />
                                    </Grid>
                                    <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusBrush}" FontFamily="Segoe UI" FontSize="13" FontWeight="SemiBold" Margin="0,8,0,4" />
                                    <TextBlock Text="{Binding DetailText}" Foreground="#A0A0B0" FontFamily="Segoe UI" FontSize="12" />
                                    <Button Content="{Binding ButtonText}" Width="100" Height="36" Command="{Binding KillCommand}" Margin="0,12,0,0" HorizontalAlignment="Right" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty State -->
                <TextBlock Grid.Row="3" Text="{Binding EmptyMessage}" Foreground="#A0A0B0" FontFamily="Segoe UI" FontSize="14" Margin="20,16,0,0" />

                <!-- Action Buttons -->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20,24,20,20">
                    <Button Content="Launch Instance" Width="200" Height="50" FontSize="16" Command="{Binding LaunchCommand}" Margin="0,0,16,0" />
                    <Button Content="Kill All" Width="140" Height="50" FontSize="16" Command="{Binding KillAllCommand}" />
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#16213E" CornerRadius="12,12,0,0" Padding="24,12">
            <DockPanel>
                <TextBlock Text="{Binding StatusText}" Foreground="#A0A0B0" FontFamily="Segoe UI" FontSize="12" DockPanel.Dock="Left" />
                <TextBlock Text="Roblox MultiInstance Bootstrapper" Foreground="#505060" FontFamily="Segoe UI" FontSize="11" DockPanel.Dock="Right" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
